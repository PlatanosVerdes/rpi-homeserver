{
  "_comment": [
    "Figures typed in by hand, with the date they were read. This is the fallback now, not the only",
    "way: tracker-control reads all four sites itself when .env carries a login for them",
    "(DIGITALCORE_USER and the rest). Without one, a site keeps whatever is written here, and every",
    "panel shows the age of the reading next to the number, because a figure with no date is worse",
    "than no figure.",
    "Neither an API key nor a passkey can replace a login: DigitalCore refuses every user endpoint",
    "to a key by name, and C411's keys are scoped to Torznab, upload and drafts with nothing for",
    "account data.",
    "Sizes are GiB, matching what the sites display. Update whenever a profile page is in front of",
    "you, or set the login and stop updating it."
  ],
  "digitalcore": {
    "read_at": "2026-08-24",
    "ratio": 6.75,
    "uploaded_gb": 78.83,
    "downloaded_gb": 11.68,
    "points": 1202.5,
    "hit_and_run": 0,
    "note": "buffer 67.15 GiB, leech bonus 19%, class Sentinel, 11 torrents seeding, no hit and run. Counted 78.83 GiB up against 53.83 GiB really uploaded (their upload multipliers) and 11.68 GiB counted down against 351.97 GiB really downloaded. The signup freeleech expired on 2026-08-14 and the counted figure has not moved since, because everything grabbed after it was 15 GiB or larger, which their rules make free anyway."
  },
  "c411": {
    "read_at": "2026-08-25",
    "ratio": 0.83,
    "uploaded_gb": 52.8,
    "downloaded_gb": 63.3,
    "note": "50 of those 52.8 GB are the signup credit, so 2.8 GB is what this account has really uploaded, up from 2.2 GB on 2026-08-21. The credit does not grow back. Downloaded has not moved since the first reading and the site shows no hit and run system, so time is the only thing owed here."
  },
  "retrotoon": {
    "read_at": "2026-08-21",
    "uploaded_gb": 0,
    "downloaded_gb": 0,
    "points": 273,
    "note": "site-wide freeleech until about 2026-09-30, so downloads cost nothing against ratio"
  }
}
